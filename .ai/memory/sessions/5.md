---
title: Session 5
date: 2026-08-17
lastModified: 2026-08-17T22:09:58+08:00
---

# Session 5

## Footer copyright name parametrized

- `SiteFooter.tsx` rendered `footer.copyright` with a hardcoded name in the
  locale strings ("© {{year}} My Name" / "© {{year}} 我的名字"). The name is
  now a `{{name}}` interpolation in both `en.json` and `zh.json`, and the
  component passes `name: t("hero.name")` alongside `year` — reusing the
  existing `hero.name` key (en "qiuxin" / zh "秋信") so the display name
  lives in exactly one place per locale.
- Same pattern as HeroSection, which already did
  `t("hero.greeting", { name: t("hero.name") })`.

## Validation

- `npx tsc --noEmit` clean. The typed `t()` derives resources from `en.json`
  via `i18next.d.ts` module augmentation, so the new required interpolation
  type-checks automatically with no declaration edits.

## Access log: Referer field (follow-up, same day)

- `WithOverallLog` (pkg/log) now always emits a `referer` attribute —
  `r.Referer()`, the empty string when the header is absent. Per user: log it
  unconditionally, not conditionally like `trace_id` (initial implementation
  omitted the key when empty; user asked for always-present). It is the first
  extra attr, before status/bytes/duration; doc comment updated.
- Scoped to the overall middleware only — the one wired in cmd/server/main.go.
  WithHTTPLog / WithSessionAwaredLog (entry logging, currently unwired) do NOT
  emit it; moving `referer` into accessLog's standard set is the follow-up if
  that's ever wanted.
- Tests: present-header case asserts the value passes through; absent-header
  case asserts the key exists with the empty string.
- Validated: `go build ./...`, `go vet ./...`, `go test ./pkg/log/` green.

## Templated systemd installer (follow-up 2, same day)

- `scripts/install-systemd-service.sh` + `scripts/personal-site.service`
  (unit template), modeled on the parent project dcna-questions'
  scripts/install-systemd-service.sh + exam-server.service. Template
  placeholders @PROJECT_DIR@ / @SERVICE_USER@ substituted via sed with
  [&|\\] escaping; hard prereq fails for systemctl, template, executable
  ./server (error message carries the full build command incl. the frontend
  build), and serverConfig.xml. Whitespace-path guard, sudo auto-detect,
  install -m 644 + daemon-reload, enable/status/journalctl next-steps.
- Adaptations vs parent: UNIT_NAME default personal-site.service;
  ExecStart=@PROJECT_DIR@/server --addr=:18972 --config-xml=serverConfig.xml
  (port per user); NO dotenv/JWT logic (login stack is gone); NO
  assets/exam-dir checks (static export is go:embed'ed into the binary);
  serverConfig.xml check kept because ExecStart loads it (kong existingfile
  would crash-loop the unit anyway).
- Validated on macOS: sh -n clean, sed render of the template verified,
  diff vs parent shows only the intended changes. No real install run — no
  systemd on macOS; needs one test on the target Linux host.
- PROCESS FUCKUPS (do not repeat): (1) a write_file payload got truncated
  mid-call leaving a broken half-script — always re-read after write when a
  call reports partial input; (2) scripts/... paths without a project-root
  prefix are AMBIGUOUS in this two-root workspace (dcna-questions also has
  scripts/) and resolved toward the parent — the user denied those writes;
  always prefix personal-site/... in tool paths. Parent verified clean via
  git status afterwards.
